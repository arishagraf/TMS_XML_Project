<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>

    <application
        android:name=".networking.App"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TMSXMLProject"
        tools:targetApi="31">
        <activity
            android:name=".networking.presentation.music.MusicActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".networking.presentation.posts.AddPostActivity"
            android:exported="false" />
        <activity
            android:name=".networking.presentation.main.MainActivity"
            android:exported="true" />
        <activity
            android:name=".networking.presentation.currency.CurrencyActivity"
            android:exported="false" />
        <activity
            android:name=".networking.presentation.posts.PostsActivity"
            android:exported="true" />
        <activity
            android:name=".networking.presentation.onboarding.OnboardingActivity"
            android:exported="true" />

        <service
            android:name=".networking.presentation.music.MusicService"
            android:enabled="true"
            android:foregroundServiceType="mediaPlayback"
            android:exported="true" />
    </application>

</manifest>